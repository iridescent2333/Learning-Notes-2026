# 引言
- 今天继续学习，
## 新增菜品
- 阿里云oss，AccessKey，KeyId，KeySecret，Endpoint(不需要写https//)，图片上传成功，前端点击上传图片，后端oss上bucket文件列表查看，代码开发2，
- SQL方言配置问题未解决，数据库列名无法自动填充，主键回显，
- 功能测试，成功，
- 最后代码上传到github出现问题，`GitHub 检测到你提交的 application-dev.yml 文件里面明文写了阿里云 OSS 的 accessKeyId 和 accessKeySecret 密钥，触发密钥保护，拒绝推送。`，把ak密钥处理一下再上传，同时也需要注意安全问题，
