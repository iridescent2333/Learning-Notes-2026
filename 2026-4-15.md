# 开端
- 今天继续学习数据库和算法设计与分析的动态规划和贪心算法，力扣刷一点题
## 数据库
### 力扣刷题(182，183)
#### 查找重复的电子邮箱
- group by a，按列名a将表中其他元素分组，
- count(a)，统计列名a中的元素个数，
- having，用于过滤分组后的结果，可以使用聚合函数(where，过滤原始行，不可使用聚合函数)，
-     SELECT email AS Email
      FROM Person
      GROUP BY email
      HAVING COUNT(email) > 1;
#### 从不订购的客户
- 
