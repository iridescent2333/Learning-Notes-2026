# 引言
- 今天继续学习苍穹外卖并复现代码功能，代码上传github地址，`https://github.com/iridescent2333/java_demo001.git`，计划暑假结束之前完成内容
## 新增员工代码实现
- 代码完善2，每次配置都要重新再员工登录用管理员登陆获得token再配置全局变量之后才能不报401，校验成功，
- 员工登录成功后会生成`JWT令牌`并响应给前端，后续请求中，前端会携带JWT令牌，通过JWT令牌可以解析出当前登录员工id，
- `hreadLocal`并不是一个Thread，而是Thread的局部变量，ThreadLocal为每个线程提供单独一份存储空间，具有线程隔离的效果，只有在线程内才能获取到对应的值，线程外则不能访问
- 再运行console可以查看客户端发送的请求是一个线程，封装成一个工具类，
- `ThreadLocal`常用方法:`public void set(T value)`设置当前线程的线程局部变量的值，`public T get()`返回当前线程所对应的线程局部变量的值，`public void remove()`移除当前线程的线程局部变量，
## 员工分页查询代码实现
- 页码，每页10条信息，搜索查询，总记录数，根据分页查询接口设计对应的DTO，后面所有的分页查询，统一都封装成PageResult对象，员工信息分页查询后端返回的对象类型为：`Result<PageResult>`，`@PostMapping`，`@GetMapping`，mp的分页查询，模糊查询，#{}，
- 功能测试，
