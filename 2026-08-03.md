# 引言
- 今天开始实现苍穹外卖项目
## 前后端联调
- 参考2026-04-24，前端页面，数据库，后端springboot，登陆成功，
- 参考2026-05-31，负载均衡，nginx，(conf文件配置)，
- 接口设计，后端开发，yapi用不了，用apifox，好用方便，导入接口成功，
- swagger，knife4j，接口文档，设置静态资源映射，swagger常用注解，增加可读性，
## 新增员工代码实现
- 需求分析，接口设计，接口，管理端发出的请求，(统一使用/admin作为前缀，用户端发出的请求，统一使用/user作为前缀)，，数据库设计，
- 代码开发，照着跟敲代码，TODO，持久层，
- 设置注解内SQL语句的自动补全，SQL方言，找到设置里面的语言与框架，然后把sql方言里面的两个方言设置成Mysql，连接database并选择数据库，使用idea专业版，连接上database即可，
- application.yml，开启驼峰命名(下划线命名和驼峰命名识别转换)，代码写入成功
- 
