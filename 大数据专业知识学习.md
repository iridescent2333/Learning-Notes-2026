### hadoop
- 大数据分布式计算处理框架，开发，运维，统计，BL，**大数据平台**，**数仓**，**实时计算**，
- 数据库与应用层之间的中间层，**大数据**，
- **HDFS**，切分和存储数据的软件，
- **mapreduce**，数据科学，map，reduce函数任务，
- **YARN**，分配服务器容器资源，
- **hive**，通过写SQL读取大数据的工具，
- **Spark**，通过内存强化性能，SQL，离线数据批处理，比hive更快，
- (**kafka**，**flink**，presto，clickhouse，es，kyuubi，ranger，alluxio)，
- **hbase**，分布式数据库，
### java/**scala**
- 语言
### SQL
- 很重要，数据库，
### docker
- 容器，
### linux
- 基本操作
### python
- 基本语法
### wireshark
- 
