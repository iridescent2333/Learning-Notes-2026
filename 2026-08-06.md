# 引言
- 今天继续苍穹外卖项目复现，
## 员工分页查询代码实现
- pagehelper(或者mybits-plus)，Page的调用，导入类，
- jwt令牌的token有有效期，有效时间，员工登录中生成新的令牌，重新设置全局参数，
- 时间格式，年月日时分秒，`@JsonFormat(pattern="yyyy-MM-dd HH:mm:ss")`(在属性上加入注解，对日期进行格式化)`private LocalDateTime updateTime;`，或者在`WebMvcConfiguration`中扩展SpringMVC的消息转换器，统一对日期类型进行格式化处理，
- 对象转换器，消息转换器，
